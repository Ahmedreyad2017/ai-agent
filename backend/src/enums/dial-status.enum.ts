export enum DialStatus {
    QUEUED = "queued",
    RINGING = "ringing",
    IN_PROGRESS = "in-progress",
    COMPLETED = "completed",
    FAILED = "failed",
    CANCELED = "canceled",
    BUSY = "busy",
    NO_ANSWER = "no-answer"
}